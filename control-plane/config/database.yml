default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  url: <%= ENV["CONTROL_PLANE_DATABASE_URL"] || ENV["DATABASE_URL"] %>

development:
  <<: *default

test:
  <<: *default
  database: control_plane_test

production:
  <<: *default
